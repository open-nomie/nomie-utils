import { Token } from './_interfaces';
/**
 *
 * Parse
 * parses a string and returns an array of
 * elements
 *
 * @param {String} str
 */
declare function parse(str?: string): Array<Token>;
export default parse;
